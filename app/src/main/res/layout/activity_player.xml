<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true">

    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:resize_mode="fit"
        app:show_buffering="when_playing"
        app:show_subtitle_button="true"
        app:show_timeout="3000"
        app:use_controller="true"
        app:animation_enabled="false"
        app:controller_layout_id="@layout/custom_player_control" />

    <!-- VLC 모드: 비디오 레이아웃 -->
    <org.videolan.libvlc.util.VLCVideoLayout
        android:id="@+id/vlcVideoLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- VLC 모드: 터치 오버레이 (제스처 감지용) -->
    <View
        android:id="@+id/vlcTouchOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone" />

    <!-- VLC 모드: 컨트롤러 컨테이너 -->
    <FrameLayout
        android:id="@+id/vlcControllerContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- VLC 모드: 버퍼링 인디케이터 -->
    <ProgressBar
        android:id="@+id/vlcBuffering"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:indeterminateTint="@android:color/white" />

    <!-- T 버튼 (F 누른 후 항상 표시) -->
    <Button
        android:id="@+id/btnSegmentEndAlways"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="60dp"
        android:layout_marginEnd="8dp"
        android:text="T"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="#80000000"
        android:padding="0dp"
        android:visibility="gone"
        android:contentDescription="구간 종료" />

    <!-- 구간 재생 순번 표시 및 네비게이션 (우측 하단) -->
    <LinearLayout
        android:id="@+id/segmentNavigationLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="4dp"
        android:layout_marginEnd="4dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone">

        <ImageButton
            android:id="@+id/btnSegmentPrevious"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="16dp"
            android:src="@android:drawable/ic_media_previous"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="이전 구간" />

        <TextView
            android:id="@+id/tvSegmentSequence"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="8dp"
            android:paddingVertical="8dp"
            android:text="1/3"
            android:textColor="#666666"
            android:textSize="16sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btnSegmentNext"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="16dp"
            android:src="@android:drawable/ic_media_next"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="다음 구간" />

    </LinearLayout>

</FrameLayout>
